# Фабрика Обогащения Данных (v2.0)
*(Ранее: "Автономный Проектный Офис")*

## 🎯 Концепция и Цель

Это state-of-the-art мульти-агентная система, спроектированная для проведения глубокого, **состязательного** исследования и генерации **верифицированного пакета бизнес-артефактов**.

Цель этого проекта — не эмулировать работу проектного офиса, а реализовать **промышленный конвейер по производству бизнес-аналитики**. Система построена на принципах нулевого доверия к AI, разделения ответственности и итеративного улучшения качества. Конечный продукт — это не просто отчет, а **аудируемый, доказуемый и устойчивый к предвзятости бизнес-кейс** для принятия инвестиционных решений.

## 🏛️ Архитектура Системы: Принцип Промышленного Конвейера

Архитектура была полностью перестроена. Монолитный агент-менеджер был заменен на многоступенчатую "Фабрику", где каждый компонент выполняет одну, узкоспециализированную операцию.

```mermaid
graph TD
    subgraph "Уровень Стратегии (Gemini 2.5 Pro)"
        A[SupervisorAgent] -->|1. Создает пару задач| B{Исследовательская Цель}
    end

    subgraph "Уровень Генерации Сырья (Gemini 2.5 Flash)"
        B -->|2a. Задача: Найти подтверждения| C[ResearcherAgent]
        B -->|2b. Задача: Найти опровержения| D[ContrarianAgent]
        C -->|3a. Пакет сырых 'Claims'| E(Сборка)
        D -->|3b. Пакет сырых 'Claims'| E
    end

    subgraph "Конвейер Контроля Качества"
        E -->|4. Весь пакет| F[BatchQualityAssessor (Flash-lite)]
        F -->|5a. 'Зеленая зона'| I[Проверка на противоречия]
        F -->|5b. 'Серая зона'| G[BatchFixerAgent (Flash)]
        F -->|5c. 'Красная зона' (Брак)| H{Отбраковка}
        G -->|6. Исправленные 'Claims'| I
    end

    subgraph "Хранилище и Синтез"
        I -->|7. NLI-аудит (Gemma)| J[Верифицированные 'Claims']
        J --> K[Knowledge Base (FAISS Index)]
        K -->|8. Вся база знаний| L[AnalystAgent (Pro)]
        L -->|9. Структурированный JSON с инсайтами| M[ReportWriterAgent (Pro)]
        M -->|10. Черновик отчета с маркерами| N[CitationPostProcessor (Python)]
        N -->|11. Финальный отчет со сносками| O[Готовые Артефакты]
    end
```

### ✨ Ключевые Архитектурные Принципы ("7 Принципов Фабрики")

1.  **Иерархическая Декомпозиция:** `SupervisorAgent` является "мозговым центром". Он не выполняет работу сам, а декомпозирует общую цель на конкретные исследовательские задачи и распределяет их.

2.  **Специализация и Состязательность Агентов:** Вместо универсальных "экспертов" мы используем узкоспециализированных "рабочих".
    *   **`ResearcherAgent`**: Ищет подтверждения.
    *   **`ContrarianAgent`**: Агрессивно ищет опровержения.
    *   **`BatchQualityAssessor`**: Оценщик качества.
    *   **`BatchFixerAgent`**: "Ремонтник" данных.
    *   **`AnalystAgent`**: Синтезатор инсайтов (генерирует JSON).
    *   **`ReportWriterAgent`**: "Копирайтер" (превращает JSON в текст).
    Этот подход гарантирует, что в систему с самого начала закладывается сбалансированная, а не однобокая картина мира.

3.  **Итеративное Улучшение:** Ни один сгенерированный AI артефакт не считается финальным. Сырые данные от "рабочих" проходят через конвейер "проверить -> исправить -> утвердить", что заменяет бинарную логику "хорошо/плохо" на производственный процесс.

4.  **Каскадная Эскалация:** Для решения задач всегда сначала используются самые дешевые ресурсы.
    *   **Оценка качества:** `Gemini 2.5 Flash-lite`.
    *   **Генерация "сырья" и исправление:** `Gemini 2.5 Flash`.
    *   **Стратегия и синтез:** `Gemini 2.5 Pro`.
    Это обеспечивает максимальную экономическую эффективность без ущерба для качества на критических этапах.

5.  **Пакетная Обработка:** Данные обрабатываются не поштучно, а большими пакетами (`batch`), чтобы снизить накладные расходы на API-вызовы и повысить пропускную способность системы.

6.  **Разделение Синтеза и Изложения:** Процесс создания нового знания (`AnalystAgent`, генерирующий JSON) полностью отделен от процесса его красивого оформления (`ReportWriterAgent`). Это кардинально снижает риск галлюцинаций в финальном отчете.

7.  **Детерминированный "Последний Штрих":** Все, что можно сделать без AI, делается без AI. `CitationPostProcessor` — простая Python-функция, которая выполняет механическую работу по расстановке сносок и генерации списка источников, гарантируя идеальное и предсказуемое форматирование.

## 🛠️ Технологический Стек

*   **Язык:** Python 3.11+
*   **AI/ML:** Google Gemini API (`Pro`, `Flash`, `Flash-lite`, `Gemma`), Pydantic
*   **Оркестрация:** Кастомный цикл в `main.py`
*   **Поиск:** Serper API
*   **Векторный Поиск:** FAISS (faiss-cpu)
*   **Зависимости:** `requests`, `python-dotenv`

## 🚀 Установка и Запуск

### 1. Предварительные требования

*   Python 3.11 или выше.
*   Git.

### 2. Установка

1.  **Клонируйте репозиторий:**
    ```bash
    git clone https://github.com/ваш-аккаунт/b2b-hr-tech-research.git
    cd b2b-hr-tech-research
    ```
2.  **Создайте и активируйте виртуальное окружение:**
    ```bash
    # Для Windows
    python -m venv venv
    .\venv\Scripts\activate

    # Для macOS/Linux
    python3 -m venv venv
    source venv/bin/activate
    ```
3.  **Установите зависимости:**
    ```bash
    pip install -r requirements.txt
    ```

### 3. Настройка API Ключей

1.  **Создайте файл `.env`** в корневой директории проекта.
2.  **Заполните файл `.env`** вашими ключами.
    ```env
    # Ключ для моделей Gemini (Pro, Flash, Gemma, Embedding)
    GOOGLE_API_KEY="AIz..."

    # Ключ для основного поискового API
    SERPER_API_KEY="...ваш ключ..."
    ```

### 4. Запуск Системы

*   **Для продолжения прерванного исследования (стандартный режим):**
    ```bash
    python main.py
    ```
*   **Для запуска нового исследования с чистого листа:**
    ```bash
    python main.py --fresh-start
    ```

## 📂 Структура Проекта

```
.
├── agents/                     # Модули AI-агентов
│   ├── models.py               # Централизованные Pydantic-модели
│   ├── supervisor.py           # "Мозг", планировщик
│   ├── worker.py               # Базовый класс для "рабочих"
│   ├── researcher.py           # Ищет подтверждения
│   ├── contrarian.py           # Ищет опровержения
│   ├── quality_assessor.py     # Контролер качества
│   ├── fixer.py                # "Ремонтник" данных
│   ├── analyst.py              # Синтезирует инсайты в JSON
│   ├── report_writer.py        # Превращает JSON в текст
│   └── search_agent.py         # Взаимодействие с поисковыми API
├── core/                       # Ядро системы
│   ├── world_model.py          # Центральное персистентное хранилище
│   ├── semantic_index.py       # Обертка для векторного индекса FAISS
│   ├── budget_manager.py       # Отслеживание API-лимитов
│   └── embedding_client.py     # Клиент для Google Embedding API
├── output/                     # Директория для всех артефактов работы
│   ├── cache/                  # Кэш поисковых запросов
│   ├── logs/                   # Детальные JSON-логи каждой транзакции
│   ├── system_state.json       # Полный слепок состояния
│   ├── faiss.index             # Векторный индекс
│   └── *.md                    # Финальные бизнес-артефакты
├── utils/                      # Вспомогательные функции
│   └── helpers.py
├── main.py                     # Главный скрипт (Оркестратор)
├── requirements.txt            # Зависимости проекта
└── README.md                   # Этот файл
```

## 📜 Финальные Артефакты

По завершении работы система генерирует полный пакет документов для принятия бизнес-решений.

| Артефакт | Создатель | Аудитория | Ценность |
| :--- | :--- | :--- | :--- |
| **`Executive_Summary_For_Director.md`** | `Analyst` + `Writer` | Руководство | Краткая выжимка с фокусом на ROI и рисках. **Все утверждения подкреплены верифицированными сносками.** |
| **`Knowledge_Base_Audit_Report.md`** | `WorldModel` (детерминированно) | Все | **"Единый Источник Правды".** Полный, читаемый список всех верифицированных фактов, использованных для генерации отчетов. |

## 📈 Возможные Улучшения (Roadmap)

*   **Панель Мониторинга (Dashboard):** Создать простой веб-интерфейс (на Streamlit) для визуализации состояния системы, прогресса выполнения плана и статистики по API-затратам в реальном времени.
*   **Расширение Инструментов Агентов:** Дать агентам возможность не только искать в вебе, но и, например, клонировать Git-репозитории для анализа кода конкурентов или взаимодействовать с API для получения структурированных данных.
*   **Внедрение Специализированных Агентов:** Добавить агентов `FinancialModelAgent` и `ProductManagerAgent` для генерации специфических артефактов (финансовых таблиц, User Stories) в рамках того же конвейера.
