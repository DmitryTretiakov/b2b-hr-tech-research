# Фабрика Состязательной Аналитики (v3.2)

## 🎯 Концепция: Борьба с Предвзятостью как Промышленный Процесс

Это не просто "мульти-агентная система". Это **промышленный конвейер для производства верифицированной, устойчивой к предвзятости бизнес-аналитики**.

Проект исходит из философии **нулевого доверия к AI**: любая информация, сгенерированная LLM, по умолчанию считается неполной, потенциально предвзятой и требующей многоступенчатой проверки. Вместо того чтобы надеяться на "разумность" одной большой модели, система реализует производственный цикл, где сырые данные проходят через состязательную генерацию, контроль качества, исправление, повторную проверку и финальный аудит на здравый смысл.

**Цель этого проекта** — не получить "отчет", а создать **аудируемый, доказуемый и воспроизводимый аналитический актив**, на основе которого можно принимать реальные инвестиционные решения.

## 🏛️ Архитектура Системы: Принцип "Фабрики"

Мы полностью отказались от модели "офиса" с универсальными менеджерами. Новая архитектура — это производственная линия с четким разделением труда, где каждый станок выполняет одну, строго определенную операцию.

```mermaid
graph TD
    subgraph "Уровень 1: Стратегия и Планирование"
        A[SupervisorAgent] -->|1. Декомпозирует цель| B(Исследовательская Задача)
    end

    subgraph "Уровень 2: Состязательная Генерация Сырья"
        B -->|2a. Найти 'ЗА'| C[ResearcherAgent]
        B -->|2b. Найти 'ПРОТИВ'| D[ContrarianAgent]
        C -->|3. Пакет 'Pro' Claims| F[Сборка]
        D -->|3. Пакет 'Contra' Claims| F
    end

    subgraph "Уровень 3: Конвейер Контроля Качества"
        F -- "Большой пакет сырых данных" --> G[BatchQualityAssessor (Flash-lite)]
        G -->|'Серая зона'| H[BatchFixerAgent (Flash)]
        H -->|Исправленный черновик| G
        G -->|"Хорошие" кандидаты| I[SanityCheckCritic (Flash)]
        I -->|"Золотой стандарт"| J[Верифицированные Claims]
    end

    subgraph "Уровень 4: Синтез и Финализация"
        J --> K[Knowledge Base (FAISS Index)]
        K -->|RAG-шлюз| L[AnalystAgent (Pro)]
        L -->|Структурированный JSON с инсайтами| M[ReportWriterAgent (Pro)]
        M -->|Текстовый черновик с маркерами [CITE:id]| N[CitationPostProcessor (Python)]
        N -->|Финальный отчет со сносками| O[Готовые Артефакты]
    end
```

### ✨ Ключевые Архитектурные Принципы ("7 Принципов Фабрики")

1.  **Иерархическая Декомпозиция:** `SupervisorAgent` является "мозговым центром". Он не выполняет работу сам, а декомпозирует общую цель на конкретные исследовательские задачи.

2.  **Специализация и Состязательность:** Вместо универсальных "экспертов" мы используем узкоспециализированных "рабочих".
    *   **`ResearcherAgent` vs `ContrarianAgent`**: Создают здоровую конкуренцию, поставляя на конвейер сбалансированную, а не однобокую информацию.
    *   **`AnalystAgent` vs `ReportWriterAgent`**: Один думает и структурирует (в JSON), второй — пишет и излагает (в Markdown).

3.  **Итеративное Улучшение:** Данные проходят через цикл **"Проверить -> Исправить -> Снова Проверить"**. Результат работы `FixerAgent` не принимается на веру, а возвращается на повторную оценку к `QualityAssessor`.

4.  **Каскадная Эскалация:** Мы используем самые дорогие ресурсы только для самых ответственных задач.
    *   **Массовая оценка:** `Flash-lite`.
    *   **Генерация и исправление:** `Flash`.
    *   **Финальный аудит и стратегия:** `Pro`.

5.  **Пакетная Обработка на Уровне Фазы:** Оркестратор обрабатывает не отдельные задачи, а **все задачи текущей фазы** одним большим пакетом. Это радикально снижает накладные расходы и повышает пропускную способность.

6.  **Разделение Синтеза и Изложения:** `AnalystAgent` не имеет доступа ко всей Базе Знаний. Он работает через **RAG-шлюз**, получая только релевантный срез данных, и генерирует на его основе структурированный JSON. Это делает процесс синтеза масштабируемым и надежным.

7.  **Детерминированный "Последний Штрих":** `CitationPostProcessor` — простая, не-AI Python-функция, которая выполняет механическую работу по расстановке сносок. Это гарантирует идеальное и предсказуемое форматирование финального документа.

## 🛠️ Технологический Стек

*   **Язык:** Python 3.11+
*   **AI/ML:** Google Gemini API (`Pro`, `Flash`, `Flash-lite`, `Gemma`), Pydantic
*   **Оркестрация:** Кастомный цикл в `main.py`
*   **Поиск:** Serper API
*   **Векторный Поиск:** FAISS (faiss-cpu)
*   **Зависимости:** `requests`, `python-dotenv`

## 🚀 Установка и Запуск

### 1. Предварительные требования

*   Python 3.11 или выше.
*   Git.

### 2. Установка

1.  **Клонируйте репозиторий:**
    ```bash
    git clone https://github.com/ваш-аккаунт/b2b-hr-tech-research.git
    cd b2b-hr-tech-research
    ```
2.  **Создайте и активируйте виртуальное окружение:**
    ```bash
    # Для Windows
    python -m venv venv
    .\venv\Scripts\activate

    # Для macOS/Linux
    python3 -m venv venv
    source venv/bin/activate
    ```
3.  **Установите зависимости:**
    ```bash
    pip install -r requirements.txt
    ```

### 3. Настройка API Ключей

1.  **Создайте файл `.env`** в корневой директории проекта.
2.  **Заполните файл `.env`** вашими ключами.
    ```env
    # Ключ для моделей Gemini (Pro, Flash, Gemma, Embedding)
    GOOGLE_API_KEY="AIz..."

    # Ключ для основного поискового API
    SERPER_API_KEY="...ваш ключ..."
    ```

### 4. Запуск Системы

*   **Для продолжения прерванного исследования (стандартный режим):**
    ```bash
    python main.py
    ```
*   **Для запуска нового исследования с чистого листа:**
    ```bash
    python main.py --fresh-start
    ```

## 📂 Структура Проекта

```
.
├── agents/                     # Модули AI-агентов
│   ├── models.py               # Централизованные Pydantic-модели
│   ├── supervisor.py           # "Мозг", планировщик
│   ├── worker.py               # Базовый класс для "рабочих"
│   ├── researcher.py           # Ищет подтверждения
│   ├── contrarian.py           # Ищет опровержения
│   ├── quality_assessor.py     # Контролер качества и SanityCheckCritic
│   ├── fixer.py                # "Ремонтник" данных
│   ├── analyst.py              # Синтезирует инсайты в JSON
│   ├── report_writer.py        # Превращает JSON в текст
│   └── search_agent.py         # Взаимодействие с поисковыми API
├── core/                       # Ядро системы
│   ├── world_model.py          # Центральное персистентное хранилище
│   ├── semantic_index.py       # Обертка для векторного индекса FAISS
│   ├── budget_manager.py       # Отслеживание API-лимитов
│   └── embedding_client.py     # Клиент для Google Embedding API
├── output/                     # Директория для всех артефактов работы
│   ├── cache/                  # Кэш поисковых запросов
│   ├── logs/                   # Детальные JSON-логи каждой транзакции
│   ├── system_state.json       # Полный слепок состояния
│   ├── faiss.index             # Векторный индекс
│   └── *.md                    # Финальные бизнес-артефакты
├── utils/                      # Вспомогательные функции
│   ├── helpers.py
│   └── prompt_engine.py        # Динамический генератор промптов
├── main.py                     # Главный скрипт (Оркестратор)
├── requirements.txt            # Зависимости проекта
└── README.md                   # Этот файл
```

## 📜 Финальные Артефакты

| Артефакт | Создатель | Аудитория | Ценность |
| :--- | :--- | :--- | :--- |
| **`Executive_Summary_For_Director.md`** | `Analyst` + `Writer` + `PostProcessor` | Руководство | Краткая выжимка с фокусом на ROI и рисках. **Все утверждения подкреплены верифицированными сносками.** |
| **`Knowledge_Base_Audit_Report.md`** | `WorldModel` (детерминированно) | Все | **"Единый Источник Правды".** Полный, читаемый список всех верифицированных фактов, использованных для генерации отчетов. |

## 📈 Возможные Улучшения (Roadmap)

*   **Панель Мониторинга (Dashboard):** Создать простой веб-интерфейс (на Streamlit) для визуализации состояния системы, прогресса выполнения плана и статистики по API-затратам в реальном времени.
*   **Расширение Инструментов Агентов:** Дать агентам возможность не только искать в вебе, но и, например, клонировать Git-репозитории для анализа кода конкурентов или взаимодействовать с API для получения структурированных данных.
*   **Внедрение Специализированных Агентов:** Добавить агентов `FinancialModelAgent` и `ProductManagerAgent` для генерации специфических артефактов (финансовых таблиц, User Stories) в рамках того же конвейера.
